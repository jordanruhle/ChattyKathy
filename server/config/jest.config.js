module.exports = {
  testEnvironment: 'node',
  roots: ['../tests'], // specify the folder containing your backend tests
  testMatch: ['**/?(*.)+(spec|test).[tj]s?(x)'], // specify file patterns for test files
};